/*
npm install express --save
we use (--save) as we don't upload node_modules to githup so any one
wantto use this project need to(npm install) so all dependencies in package.json
will be downloaded
 */